# X1 · 手写系列合集

> 热度:🔥🔥🔥

高频手写题一站式。每题给出核心实现与要点。

## 1. 手写 new

```js
function myNew(Constructor, ...args) {
  const obj = Object.create(Constructor.prototype);  // ①创建对象并链接原型
  const result = Constructor.apply(obj, args);       // ②执行构造函数,this 指向 obj
  return result instanceof Object ? result : obj;    // ③返回对象或 obj
}
```
**要点**:构造函数若显式返回对象,则用该对象;否则返回新建的 obj。

## 2. 手写 instanceof

```js
function myInstanceof(left, right) {
  if (typeof left !== 'object' || left === null) return false;
  let proto = Object.getPrototypeOf(left);
  while (proto) {
    if (proto === right.prototype) return true;
    proto = Object.getPrototypeOf(proto);
  }
  return false;
}
```

## 3. 数组扁平化 flatten

```js
// 递归
function flatten(arr) {
  return arr.reduce(
    (acc, cur) => acc.concat(Array.isArray(cur) ? flatten(cur) : cur),
    []
  );
}
// 原生:arr.flat(Infinity)
```

## 4. 数组去重

```js
const unique = arr => [...new Set(arr)];

// 对象数组按 key 去重
function uniqueBy(arr, key) {
  const map = new Map();
  return arr.filter(item => !map.has(item[key]) && map.set(item[key], 1));
}
```

## 5. 柯里化 curry

```js
function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) {
      return fn.apply(this, args);           // 参数够了就执行
    }
    return (...next) => curried.apply(this, [...args, ...next]); // 否则继续收集
  };
}
// const add = curry((a, b, c) => a + b + c);
// add(1)(2)(3) === add(1, 2)(3) === 6
```

## 6. 发布订阅 EventEmitter

```js
class EventEmitter {
  constructor() { this.events = {}; }
  on(type, cb) {
    (this.events[type] ||= []).push(cb);
    return this;
  }
  emit(type, ...args) {
    (this.events[type] || []).forEach(cb => cb(...args));
    return this;
  }
  off(type, cb) {
    if (this.events[type]) {
      this.events[type] = this.events[type].filter(fn => fn !== cb);
    }
    return this;
  }
  once(type, cb) {
    const wrap = (...args) => { cb(...args); this.off(type, wrap); };
    return this.on(type, wrap);
  }
}
```

## 7. 手写 Promise.all(见 J7)

```js
Promise.myAll = promises =>
  new Promise((resolve, reject) => {
    const res = [];
    let count = 0;
    promises.forEach((p, i) => {
      Promise.resolve(p).then(v => {
        res[i] = v;
        if (++count === promises.length) resolve(res);
      }, reject);
    });
  });
```

## 8. 深拷贝(见 J9)

```js
function deepClone(target, map = new WeakMap()) {
  if (target === null || typeof target !== 'object') return target;
  if (map.has(target)) return map.get(target);
  const clone = Array.isArray(target) ? [] : {};
  map.set(target, clone);
  Reflect.ownKeys(target).forEach(k => (clone[k] = deepClone(target[k], map)));
  return clone;
}
```

## 9. 防抖 / 节流(见 J10)

```js
const debounce = (fn, delay) => {
  let t;
  return function (...args) {
    clearTimeout(t);
    t = setTimeout(() => fn.apply(this, args), delay);
  };
};

const throttle = (fn, gap) => {
  let last = 0;
  return function (...args) {
    const now = Date.now();
    if (now - last >= gap) { last = now; fn.apply(this, args); }
  };
};
```

## 10. 实现 Object.create

```js
function myCreate(proto) {
  function F() {}
  F.prototype = proto;
  return new F();
}
```

## 11. 千分位格式化

```js
const format = n => n.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ',');
// format(1234567) → '1,234,567'
```

## 12. 手写 Array.prototype.reduce

```js
Array.prototype.myReduce = function (cb, initial) {
  let acc = initial;
  let start = 0;
  if (acc === undefined) { acc = this[0]; start = 1; }  // 无初值用首元素
  for (let i = start; i < this.length; i++) {
    acc = cb(acc, this[i], i, this);
  }
  return acc;
};
```

## 一句话背诵版

**new(Object.create+apply+返回判断)、instanceof(沿原型链找 prototype)、flatten(reduce 递归)、curry(参数够则执行否则收集)、发布订阅(events 对象存回调数组)、Promise.all(计数+按序存)、深拷贝(WeakMap 防循环)——这几道是手写高频必备。**
